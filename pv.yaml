apiVersion: v1
kind: PersistentVolume
metadata:
  name: pipeline4-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/pipeline4
